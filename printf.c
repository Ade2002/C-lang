#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int	main(int argc, char **argv)
{
    printf("Ade age is %f \n", 20.1);
    printf("Ade %s is %f \n", "age", 20.1);
    printf("%f \n", 5.4 + 5.8);
    printf("%f \n", pow(2,3));
    printf("%f \n", sqrt(36));
    system("pause");
    return 0;
}
